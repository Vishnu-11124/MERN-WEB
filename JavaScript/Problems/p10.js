// Problem: Given an array of numbers, use the map function to create a new array that contains each number multiplied by 10.

let numbers = [1, 2, 3, 4, 5];

let multipledByTen = numbers.map((num) => num * 10);

console.log(multipledByTen); // Output: [10, 20, 30, 40, 50]