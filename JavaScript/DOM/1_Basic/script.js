/*
  -> DOM stands for Document Object Model.

    It’s a programming interface that represents a web page as a tree of objects — so JavaScript can interact with and manipulate the content, structure, and style of your webpage dynamically.



*/ 

// -----------------------------------------------------------------------------------------------------

// console.dir(document) is a JavaScript debugging command that prints the entire DOM object (the document) as a JavaScript object — so you can inspect all of its properties and methods in your browser’s console.
console.dir(document) 

console.log(document.body) 
console.log(document.head)
console.log(document.documentURI) //